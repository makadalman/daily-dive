import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    primary: {
      main: "#6896fc"
    },
    secondary: {
      main: "#adc2f0"
    }
  },
  spacing: 8,
});

export default theme;